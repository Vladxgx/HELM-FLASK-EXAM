apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "hello-newapp.fullname" . }}
  labels:
    {{- include "hello-newapp.labels" . | nindent 4 }}
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      {{- include "hello-newapp.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "hello-newapp.selectorLabels" . | nindent 8 }}
    spec:
      containers:
        - name: {{ include "hello-newapp.name" . }}
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          ports:
            - name: http
              containerPort: 8000
              protocol: TCP
          resources:
            {{- toYaml .Values.resources | nindent 12 }}
